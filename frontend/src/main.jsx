import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';

// NOTE: This is a Phase-0 scaffold placeholder. Routing, context providers
// (auth, sync status, etc.) will be wired in here during later phases.
// See docs/PROGRESS.md for the build sequence.

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
